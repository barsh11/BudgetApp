import React from 'react';
import Backdrop from './Backdrop';

export default {
  title: 'AcrossApp/Backdrop',
};

export const common = () => (
  <Backdrop clicked={() => {}} open>
    common Backdrop
  </Backdrop>
);
