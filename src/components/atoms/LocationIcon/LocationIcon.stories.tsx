import React from 'react';
import LocationIcon from './LocationIcon';

export default {
  title: 'Transactions/LocationIcon',
};

export const common = () => <LocationIcon clicked={() => {}}>common LocationIcon</LocationIcon>;
