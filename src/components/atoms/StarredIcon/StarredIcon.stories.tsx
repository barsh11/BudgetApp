import React from 'react';
import StarredIcon from './StarredIcon';

export default {
  title: 'Transactions/StarredIcon',
};

export const common = () => (
  <StarredIcon isStarred clicked={() => ''}>
    common StarredIcon
  </StarredIcon>
);
