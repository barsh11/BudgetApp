import React from 'react';
import ErrorMessage from './ErrorMessage';

export default {
  title: 'AcrossApp/ErrorMessage',
};

export const common = () => <ErrorMessage errorMsg="something went wrong." />;
